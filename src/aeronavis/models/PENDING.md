# aeronavis.models — PENDING

- Whether velocity & pseudo-odo share one backbone with two heads
  (assumption: shared trunk, separate heads; revisit after Part 5 trials).
- `model.max_params=500000` budget interpretation (see models/PENDING.md).
- Visual-odo feature detector choice (ORB vs learned) — benchmark in Part 7.