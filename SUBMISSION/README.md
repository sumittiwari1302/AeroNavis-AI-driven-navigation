# SUBMISSION/ — final SIH 2026 deliverables

Everything the judges/presenters consume goes here at the end: packaged model
bundle, frozen `config.yaml`, inference entrypoint, demo video, and the response
to SIH26168's evaluation plan. Gitignored content stays out of the repo; keep
only notes/manifest committed via `.gitkeep`.

**How to run**: packaging steps arrive with the finalization part; empty in
Part 0.