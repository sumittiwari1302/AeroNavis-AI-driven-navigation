# benchmarks/ — reproducible comparative runs

Hosts runnable benchmark definitions (loaders, protocols) and their output
tables/figures comparing AeroNavis variants and baselines (IMU-only, pure GNSS).
Dry-run defined in Part 10 (`make bench`).

**How to run** (Part 10): `make bench`. Not implemented in Part 0.